<%= form_with(model: @setting, local: true) do |form| %>
  <div class="field">
    <%= form.label :locale %>
    <%= form.select :locale, Setting.locales.keys %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>