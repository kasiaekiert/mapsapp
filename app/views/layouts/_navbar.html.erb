<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="#">MapApp</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavDropdown">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <%= link_to t('.home'), root_path, class: 'nav-link'%>
      </li>
      <li class="nav-item">
        <%= link_to t('.events'), events_path, class: 'nav-link'%>
      </li>
      <li class="nav-item">
        <%= link_to t('.places'), places_path, class: 'nav-link'%>
      </li>
    </ul>
  </div>

      <% if user_signed_in? %>
        <span class="navbar-text">
        <span class="glyphicon glyphicon-user"></span> <%= current_user.email %></span>
        <%= link_to t('.logout'), destroy_user_session_path, method: :delete, :class => 'btn btn-default btn-sm glyphicon glyphicon-log-out'%>
      <% else %>
        <%= link_to t('.login'), new_user_session_path, :class => 'btn btn-default btn-sm glyphicon glyphicon-log-in'%>
      <% end %>
  </div>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>

</nav>